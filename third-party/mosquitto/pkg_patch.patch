diff -aur mosquitto-2.0.15/plugins/auth-by-ip/Makefile ../build/extbld/third_party/cmd/mosquitto/mosquitto-2.0.15/plugins/auth-by-ip/Makefile
--- mosquitto-2.0.15/plugins/auth-by-ip/Makefile	2022-08-16 09:32:59.000000000 -0400
+++ ../build/extbld/third_party/cmd/mosquitto/mosquitto-2.0.15/plugins/auth-by-ip/Makefile	2023-02-26 17:33:14.866814078 -0500
@@ -1,27 +1,12 @@
 include ../../config.mk
 
-.PHONY : all binary check clean reallyclean test install uninstall
+.PHONY : all binary
 
 PLUGIN_NAME=mosquitto_auth_by_ip
 
 all : binary
 
-binary : ${PLUGIN_NAME}.so
+binary : ${PLUGIN_NAME}.o
 
-${PLUGIN_NAME}.so : ${PLUGIN_NAME}.c
-	$(CROSS_COMPILE)$(CC) $(PLUGIN_CPPFLAGS) $(PLUGIN_CFLAGS) $(PLUGIN_LDFLAGS) -fPIC -shared $< -o $@
-
-reallyclean : clean
-clean:
-	-rm -f *.o ${PLUGIN_NAME}.so *.gcda *.gcno
-
-check: test
-test:
-
-install: ${PLUGIN_NAME}.so
-	# Don't install, these are examples only.
-	#$(INSTALL) -d "${DESTDIR}$(libdir)"
-	#$(INSTALL) ${STRIP_OPTS} ${PLUGIN_NAME}.so "${DESTDIR}${libdir}/${PLUGIN_NAME}.so"
-
-uninstall :
-	-rm -f "${DESTDIR}${libdir}/${PLUGIN_NAME}.so"
+${PLUGIN_NAME}.o : ${PLUGIN_NAME}.c
+	$(CROSS_COMPILE)$(CC) $(PLUGIN_CPPFLAGS) $(PLUGIN_CFLAGS) $(PLUGIN_LDFLAGS) $< -o $@
diff -aur mosquitto-2.0.15/plugins/deny-protocol-version/Makefile ../build/extbld/third_party/cmd/mosquitto/mosquitto-2.0.15/plugins/deny-protocol-version/Makefile
--- mosquitto-2.0.15/plugins/deny-protocol-version/Makefile	2022-08-16 09:32:59.000000000 -0400
+++ ../build/extbld/third_party/cmd/mosquitto/mosquitto-2.0.15/plugins/deny-protocol-version/Makefile	2023-02-26 17:33:14.866814078 -0500
@@ -8,22 +8,7 @@
 
 all : binary
 
-binary : ${PLUGIN_NAME}.so
+binary : ${PLUGIN_NAME}.o
 
-${PLUGIN_NAME}.so : ${PLUGIN_NAME}.c
-	$(CROSS_COMPILE)$(CC) $(PLUGIN_CPPFLAGS) $(PLUGIN_CFLAGS) $(PLUGIN_LDFLAGS) -fPIC -shared $< -o $@
-
-reallyclean : clean
-clean:
-	-rm -f *.o ${PLUGIN_NAME}.so *.gcda *.gcno
-
-check: test
-test:
-
-install: ${PLUGIN_NAME}.so
-	# Don't install, these are examples only.
-	#$(INSTALL) -d "${DESTDIR}$(libdir)"
-	#$(INSTALL) ${STRIP_OPTS} ${PLUGIN_NAME}.so "${DESTDIR}${libdir}/${PLUGIN_NAME}.so"
-
-uninstall :
-	-rm -f "${DESTDIR}${libdir}/${PLUGIN_NAME}.so"
+${PLUGIN_NAME}.o : ${PLUGIN_NAME}.c
+	$(CROSS_COMPILE)$(CC) $(PLUGIN_CPPFLAGS) $(PLUGIN_CFLAGS) $(PLUGIN_LDFLAGS) $< -o $@
diff -aur mosquitto-2.0.15/plugins/message-timestamp/Makefile ../build/extbld/third_party/cmd/mosquitto/mosquitto-2.0.15/plugins/message-timestamp/Makefile
--- mosquitto-2.0.15/plugins/message-timestamp/Makefile	2022-08-16 09:32:59.000000000 -0400
+++ ../build/extbld/third_party/cmd/mosquitto/mosquitto-2.0.15/plugins/message-timestamp/Makefile	2023-02-26 17:33:14.866814078 -0500
@@ -1,27 +1,12 @@
 include ../../config.mk
 
-.PHONY : all binary check clean reallyclean test install uninstall
+.PHONY : all binary
 
 PLUGIN_NAME=mosquitto_message_timestamp
 
 all : binary
 
-binary : ${PLUGIN_NAME}.so
+binary : ${PLUGIN_NAME}.o
 
-${PLUGIN_NAME}.so : ${PLUGIN_NAME}.c
-	$(CROSS_COMPILE)$(CC) $(PLUGIN_CPPFLAGS) $(PLUGIN_CFLAGS) $(PLUGIN_LDFLAGS) -fPIC -shared $< -o $@
-
-reallyclean : clean
-clean:
-	-rm -f *.o ${PLUGIN_NAME}.so *.gcda *.gcno
-
-check: test
-test:
-
-install: ${PLUGIN_NAME}.so
-	# Don't install, these are examples only.
-	#$(INSTALL) -d "${DESTDIR}$(libdir)"
-	#$(INSTALL) ${STRIP_OPTS} ${PLUGIN_NAME}.so "${DESTDIR}${libdir}/${PLUGIN_NAME}.so"
-
-uninstall :
-	-rm -f "${DESTDIR}${libdir}/${PLUGIN_NAME}.so"
+${PLUGIN_NAME}.o : ${PLUGIN_NAME}.c
+	$(CROSS_COMPILE)$(CC) $(PLUGIN_CPPFLAGS) $(PLUGIN_CFLAGS) $(PLUGIN_LDFLAGS) $< -o $@
diff -aur mosquitto-2.0.15/plugins/payload-modification/Makefile ../build/extbld/third_party/cmd/mosquitto/mosquitto-2.0.15/plugins/payload-modification/Makefile
--- mosquitto-2.0.15/plugins/payload-modification/Makefile	2023-02-26 19:55:15.996788407 -0500
+++ ../build/extbld/third_party/cmd/mosquitto/mosquitto-2.0.15/plugins/payload-modification/Makefile	2023-02-26 21:42:39.338024192 -0500
@@ -1,27 +1,12 @@
 include ../../config.mk
 
-.PHONY : all binary check clean reallyclean test install uninstall
+.PHONY : all binary
 
 PLUGIN_NAME=mosquitto_payload_modification
 
 all : binary
 
-binary : ${PLUGIN_NAME}.so
+binary : ${PLUGIN_NAME}.o
 
-${PLUGIN_NAME}.so : ${PLUGIN_NAME}.c
-	$(CROSS_COMPILE)$(CC) $(PLUGIN_CPPFLAGS) $(PLUGIN_CFLAGS) $(PLUGIN_LDFLAGS) -fPIC -shared $< -o $@
-
-reallyclean : clean
-clean:
-	-rm -f *.o ${PLUGIN_NAME}.so *.gcda *.gcno
-
-check: test
-test:
-
-install: ${PLUGIN_NAME}.so
-	# Don't install, these are examples only.
-	#$(INSTALL) -d "${DESTDIR}$(libdir)"
-	#$(INSTALL) ${STRIP_OPTS} ${PLUGIN_NAME}.so "${DESTDIR}${libdir}/${PLUGIN_NAME}.so"
-
-uninstall :
-	-rm -f "${DESTDIR}${libdir}/${PLUGIN_NAME}.so"
+${PLUGIN_NAME}.o : ${PLUGIN_NAME}.c
+	$(CROSS_COMPILE)$(CC) $(PLUGIN_CPPFLAGS) $(PLUGIN_CFLAGS) $(PLUGIN_LDFLAGS) $< -o $@
