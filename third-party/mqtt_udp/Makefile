PKG_SOURCES := https://github.com/dzavalishin/mqtt_udp.git

# PKG_PATCHES := mqtt_udp.patch

# include $(EXTBLD_LIB)

$(CONFIGURE) :
	touch $@

$(BUILD) :
	touch $@

$(INSTALL) :
	cp $(PKG_SOURCE_DIR)/lang/c/* $(PKG_INSTALL_DIR)
	touch $@
