diff -aur boost_1_54_0/boost/filesystem/config.hpp boost/boost/filesystem/config.hpp
--- boost_1_54_0/boost/filesystem/config.hpp	2012-05-21 17:44:43.000000000 +0400
+++ boost/boost/filesystem/config.hpp	2013-09-25 15:08:43.245397701 +0400
@@ -43,7 +43,9 @@
 //  The immediate fix was to throw directly, wrapped in a macro to make any later change
 //  easier.
 
-#define BOOST_FILESYSTEM_THROW(EX) throw EX
+#include <boost/throw_exception.hpp>
+
+#define BOOST_FILESYSTEM_THROW(EX) BOOST_THROW_EXCEPTION(EX)
 
 # if defined( BOOST_NO_STD_WSTRING )
 #   error Configuration not supported: Boost.Filesystem V3 and later requires std::wstring support
diff -aur boost_1_54_0/boost/filesystem/operations.hpp boost/boost/filesystem/operations.hpp
--- boost_1_54_0/boost/filesystem/operations.hpp	2012-05-21 17:44:43.000000000 +0400
+++ boost/boost/filesystem/operations.hpp	2013-09-25 14:40:15.103040398 +0400
@@ -990,11 +990,11 @@
       const std::string & what_arg, system::error_code ec)
       : system::system_error(ec, what_arg)
     {
-      try
+      //try
       {
         m_imp_ptr.reset(new m_imp);
       }
-      catch (...) { m_imp_ptr.reset(); }
+      //      catch (...) { m_imp_ptr.reset(); }
     }
 
     filesystem_error(
@@ -1002,12 +1002,12 @@
       system::error_code ec)
       : system::system_error(ec, what_arg)
     {
-      try
+      //try
       {
         m_imp_ptr.reset(new m_imp);
         m_imp_ptr->m_path1 = path1_arg;
       }
-      catch (...) { m_imp_ptr.reset(); }
+      //      catch (...) { m_imp_ptr.reset(); }
     }
     
     filesystem_error(
@@ -1015,13 +1015,13 @@
       const path& path2_arg, system::error_code ec)
       : system::system_error(ec, what_arg)
     {
-      try
+      //try
       {
         m_imp_ptr.reset(new m_imp);
         m_imp_ptr->m_path1 = path1_arg;
         m_imp_ptr->m_path2 = path2_arg;
       }
-      catch (...) { m_imp_ptr.reset(); }
+      //      catch (...) { m_imp_ptr.reset(); }
     }
 
     ~filesystem_error() throw() {}
@@ -1042,7 +1042,7 @@
       if (!m_imp_ptr.get())
         return system::system_error::what();
 
-      try
+      //try
       {
         if (m_imp_ptr->m_what.empty())
         {
@@ -1062,7 +1062,7 @@
         }
         return m_imp_ptr->m_what.c_str();
       }
-      catch (...)
+      //catch (...)
       {
         return system::system_error::what();
       }
