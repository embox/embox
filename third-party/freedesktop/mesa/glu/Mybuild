package third_party.freedesktop.mesa

@BuildDepends(third_party.freedesktop.mesa.osmesa)
@Build(script="$(EXTERNAL_MAKE)")
module  libglu_osmesa {
	source "^BUILD/extbld/^MOD_PATH/install/libglu.a"
}
