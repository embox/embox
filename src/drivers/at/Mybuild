package embox.driver.at

module at_parser {
	source "at_parser.c"
}

module at_emulator {
	source "emulator/at_emulator.c"

	depends at_parser
}

module at_client {
	source "at_client.c"

	depends at_parser 
	depends embox.compat.posix.LibPosix  
}
