package embox.driver.input.keyboard

module keyboard {
	source "keyboard.c"
	source "keymap.c"

	option number register_in_vt = 1

	depends embox.driver.input.core
}

module diag {
	depends keyboard
}
