
package embox.compat.posix.proc

module All {

	depends exec,
		fork,
		wait,
		//signal,
		exit,
		uid,
		nice,
		priority,
		pid
}

static module exec {
	source "exec.c"

	depends embox.util.LibUtil
}

static module fork {
	source "fork.c"

	depends embox.kernel.task.kfork
	depends embox.util.LibUtil
}

static module wait {
	source "wait.c"

	depends embox.util.LibUtil
}

static module signal {
	source "signal.c"

	depends embox.kernel.thread.signal
	depends sigset
}

static module sigset {
	source "sigset.c"
	depends embox.util.Bitmap
}

static module exit {
	source "exit.c"

	depends embox.util.LibUtil
}

static module uid {
	source "uid.c"

	depends embox.kernel.task.u_area
}

static module nice {
	source "nice.c"

	depends priority
}

static module priority {
	source "priority.c"
}

static module pid {
	source "pid.c"
}
