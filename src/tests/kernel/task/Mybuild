package embox.test.kernel.task

module signal {
	source "signal.c"

	depends embox.kernel.task.api
	depends embox.framework.LibFramework
	depends embox.kernel.task.signal
}

module idx_desc_test {
	source "idx_desc_test.c"

	depends embox.kernel.task.api
	depends embox.framework.LibFramework
}

module pipe_test {
	source "pipe_test.c"
	depends embox.compat.posix.LibPosixIdx
}
