package embox.test.util

module array_test {
	source "array_test.c"

	depends embox.framework.LibFramework
}

module bit_test {
	source "bit_test.c"

	depends embox.framework.LibFramework
}

module bitmap_test {
	source "bitmap_test.c"

	@NoRuntime depends embox.util.LibUtil
	depends embox.framework.LibFramework
}

module slist_test {
	source "slist_test.c"

	@NoRuntime depends embox.util.LibUtil
	depends embox.framework.LibFramework
}

module tree_test {
	source "tree_test.c"

	depends embox.framework.LibFramework
}

module ring_buff_test {
	source "ring_buff_test.c"

	@NoRuntime depends embox.util.LibUtil
	depends embox.framework.LibFramework
}

module hashtable_test {
	source "hashtable_test.c"

	@NoRuntime depends embox.util.LibUtil
	depends embox.framework.LibFramework
}

module dlist_test {
	source "dlist_test.c"

	depends embox.framework.LibFramework
}

module indexator_test {
	source "indexator_test.c"

	@NoRuntime depends embox.util.LibUtil
	depends embox.framework.LibFramework
}

module msg_buff_test {
	source "msg_buff_test.c"

	@NoRuntime depends embox.util.LibUtil
	depends embox.framework.LibFramework
}
