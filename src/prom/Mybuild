package embox.prom

abstract module printf { }

module prom_printf extends printf {
	source "prom_printf.c", "prom_printf.h"

	@NoRuntime depends embox.driver.diag.diag_api
	@NoRuntime depends embox.compat.libc.Print
}

module noprom_printf extends printf {
	source "noprom_printf.h"
}
