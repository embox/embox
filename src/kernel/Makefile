OBJS-$(MONITOR_KERNEL) += main.o
OBJS-$(MONITOR_SYSTEM_IRQ) += irq.o
OBJS-$(MONITOR_SYSTEM_TIMERS) += timers_common.o
OBJS-$(MONITOR_KERNEL) += init.o
OBJS-$(MONITOR_SOFT_IRQ) += softirq.o

DEFS-y +=__KERNEL__

include $(SCRIPTS_DIR)/common.mk
