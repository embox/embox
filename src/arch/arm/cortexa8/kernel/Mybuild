
package embox.arch.arm.cortexa8.kernel

module context extends embox.arch.context {
	source "context.c", "context.h", "context_frame.S"
	source "context_switch.S"
}

module locore extends embox.arch.locore {

	option number irq_stack_size = 0x100

	source "entry.S"
	source "exception_table.S"

	depends embox.arch.arm.kernel.head
}

module interrupt extends embox.arch.interrupt {
	source "ipl_impl.h"
	depends locore
}

module arch {
	source "arch.c"
}
