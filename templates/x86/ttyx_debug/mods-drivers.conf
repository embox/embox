mod(embox.driver.interrupt.i8259)
mod(embox.driver.clock.pit)

mod(embox.driver.terminal)
mod(embox.driver.video.vga_console)
mod(embox.driver.keyboard)

#mod(embox.driver.tty_factory)
mod(embox.driver.tty_kbd)
#mod(embox.driver.ttyo)
#mod(embox.driver.ttyio)
#mod(embox.driver.ttys)
#mod(embox.driver.ttyp)
