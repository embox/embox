mod(embox.driver.interrupt.apic)
mod(embox.driver.clock.pit)

mod(embox.driver.terminal)
mod(embox.driver.vconsole)
mod(embox.driver.tty)

mod(embox.driver.serial.serial_port)
mod(embox.driver.keyboard)
