/*
 * Linkage configuration.
 */

/* region (origin, length) */
RAM (0xffffffff80000000, 128M)
// ROM (0xa0000000, 64M)

/* section (region[, lma_region]) */
text   (RAM)
rodata (RAM)
data   (RAM)
bss    (RAM)
