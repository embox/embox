/*
 * Linkage configuration.
 */

/* region (origin, length) */
ROM (0x00000000, 1M)
RAM (0x20000200, 512K)

/* section (region[, lma_region]) */
text   (ROM)
rodata (ROM)
data   (RAM, ROM)
bss    (RAM)
