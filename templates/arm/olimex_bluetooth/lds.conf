/*
 * Linkage configuration.
 */

/* region (origin, length) */
ROM (0x100000, 256K)
RAM (0x200000, 64K)

/* section (region[, lma_region]) */
text   (ROM)
rodata (ROM)
data   (RAM, ROM)
bss    (RAM)
