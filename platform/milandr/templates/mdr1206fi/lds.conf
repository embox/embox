/*
 * Linkage configuration.
 */

/* region (origin, length) */
ROM (0x10000000, 512K)
RAM (0x20000000, 112K)

/* section (region[, lma_region]) */
text   (ROM)
rodata (ROM)
data   (RAM, ROM)
bss    (RAM)
